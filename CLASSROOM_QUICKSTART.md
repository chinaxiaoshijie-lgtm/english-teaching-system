# 上课模式快速开始指南

## 功能概述

上课模式允许教师上传PDF课件后，开始一个实时同步的课堂会话。教师翻页时，所有在线学生端的幻灯片将实时同步更新。

## 前置条件

### 1. 安装Canvas包

上课模式需要Canvas包来处理PDF文件。请按照以下步骤安装：

```bash
npm install canvas
```

**Windows用户**：如果安装失败，请确保已安装Visual Studio Build Tools。

详细安装说明请参考 [INSTALL_CANVAS.md](INSTALL_CANVAS.md)

### 2. 验证安装

```bash
node -e "console.log(require('canvas'))"
```

如果未报错，说明安装成功。

## 使用步骤

### 教师端操作

#### 1. 上传PDF课件

1. 登录教师端：`http://[教师IP]:3000/teacher`
2. 点击"课件管理"标签
3. 点击"上传课件"按钮
4. 选择PDF文件（.pdf扩展名）
5. 输入课件标题（可选）
6. 点击上传

**注意**：上传时会自动处理PDF并生成幻灯片，这可能需要几秒钟，请等待处理完成。

#### 2. 开始上课

1. 点击"上课模式"标签
2. 从下拉菜单中选择一个PDF课件
3. 点击"开始上课"按钮

此时，所有已登录的学生端将收到上课通知并自动显示课件。

#### 3. 翻页

- 点击"上一页"按钮：回到上一页幻灯片
- 点击"下一页"按钮：跳转到下一页幻灯片

所有学生的幻灯片将实时同步更新。

#### 4. 结束上课

点击"结束上课"按钮，课堂会话结束，所有学生端的上课视图将关闭。

### 学生端操作

#### 1. 登录学生端

1. 访问学生端：`http://[教师IP]:3000/student`
2. 输入姓名和学号
3. 点击"加入课堂"

#### 2. 自动同步

- 如果教师端已经开始上课，学生登录后会自动进入上课模式
- 顶部会显示"上课中"提示和课件标题
- 自动显示当前幻灯片

#### 3. 实时更新

当教师翻页时，学生端的幻灯片会自动更新，无需手动操作。

## 界面说明

### 教师端界面

```
┌─────────────────────────────────────────┐
│  上课模式                               │
│                                          │
│  [选择课件] [开始上课] [结束上课]        │
│                                          │
│  当前课件：xxx.pdf                       │
│  课堂ID：123                             │
│                                          │
│  ┌─────────────────────────────────┐    │
│  │                                 │    │
│  │        幻灯片显示区域            │    │
│  │                                 │    │
│  │                                 │    │
│  └─────────────────────────────────┘    │
│                                          │
│        [上一页]  1 / 10  [下一页]        │
└─────────────────────────────────────────┘
```

### 学生端界面

```
┌─────────────────────────────────────────┐
│  上课中 - xxx.pdf                        │
│                                          │
│  ┌─────────────────────────────────┐    │
│  │                                 │    │
│  │        幻灯片显示区域            │    │
│  │                                 │    │
│  │                                 │    │
│  └─────────────────────────────────┘    │
│                                          │
│  [课件] [任务] [我的提交]                 │
└─────────────────────────────────────────┘
```

## 常见问题

### Q: 上传PDF后看不到幻灯片？

**A**: 请检查：
1. Canvas包是否正确安装
2. 上传的是否为PDF文件（.pdf扩展名）
3. 查看服务器日志是否有错误信息
4. 确保上传的PDF文件没有损坏

### Q: 学生端看不到上课通知？

**A**: 请检查：
1. 学生是否已登录
2. Socket.IO连接是否正常（查看连接状态）
3. 教师端是否成功开始上课
4. 网络连接是否正常

### Q: 翻页后学生端没有同步？

**A**: 请检查：
1. 学生端的Socket.IO连接是否正常
2. 是否有网络延迟或中断
3. 学生端是否在课堂标签页

### Q: Canvas包安装失败？

**A**: 请参考 [INSTALL_CANVAS.md](INSTALL_CANVAS.md) 中的故障排除部分。

### Q: 可以使用PPT文件吗？

**A**: 当前版本仅支持PDF文件。PPTX文件需要先转换为PDF格式。

## 技术细节

### 文件存储

上传的PDF文件和生成的幻灯片存储位置：

```
uploads/lessons/
  └── {lessonId}/
      ├── original.pdf           # 原始PDF文件
      └── slides/                # 幻灯片目录
          ├── slide_1.jpg        # 第1页幻灯片
          ├── slide_2.jpg        # 第2页幻灯片
          └── ...
```

### 数据存储

课堂会话和幻灯片信息存储在SQLite数据库中：

- `classroom_sessions`：课堂会话记录
- `lesson_slides`：幻灯片记录
- `lessons`：课件记录（包含slide_count和has_slides字段）

### 通信协议

教师端和学生端通过Socket.IO实时通信：

- `class:started`：上课开始
- `class:ended`：上课结束
- `page:changed`：页面变化
- `class:active`：活跃课堂会话

## 性能建议

1. **PDF文件大小**：建议PDF文件不超过50MB
2. **幻灯片数量**：建议幻灯片数量不超过100页
3. **网络带宽**：建议使用千兆局域网
4. **同时在线学生数**：建议不超过60人

## 安全建议

1. 仅在局域网环境中使用
2. 不要上传敏感或机密内容
3. 定期清理过期的课件和幻灯片文件

## 后续功能

计划中的功能：
- PPTX文件支持
- 幻灯片批注功能
- 全屏显示模式
- 幻灯片缩放功能
- 离线模式支持

## 技术支持

如遇到问题，请检查：
1. 服务器日志
2. 浏览器控制台错误
3. 网络连接状态
4. Canvas包安装状态

更多详细信息请参考 [CHANGELOG.md](CHANGELOG.md) 和 [README.md](README.md)。
